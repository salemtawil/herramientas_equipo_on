# tools/trash

Esta carpeta contiene scripts archivados y referencias antiguas.

- No forma parte del runtime activo registrado en `app.py`.
- No debe usarse como fuente de verdad para cambios de produccion.
- Si un script de aqui vuelve a ser necesario, conviene moverlo a una ubicacion activa y revisarlo antes de reutilizarlo.
