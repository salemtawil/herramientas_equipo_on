services:
  - type: web
    name: herramientas-equipo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: herramientas-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1